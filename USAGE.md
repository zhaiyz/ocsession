# OpenCode Session Manager 使用指南

## 🚀 启动应用

```bash
cd /Users/zhaiyazhou/code/ocsession
./bin/ocsession
```

## ⌨️ 键盘快捷键

| 快捷键 | 功能 | 说明 |
|--------|------|------|
| `↑` 或 `k` | 向上移动 | 选择上一个会话 |
| `↓` 或 `j` | 向下移动 | 选择下一个会话 |
| `/` | 搜索模式 | 进入搜索模式 |
| `Enter` | 继续会话 | 切换到 OpenCode 继续选中的会话 |
| `r` | 刷新列表 | 重新加载会话列表 |
| `q` 或 `Ctrl+C` | 退出 | 退出程序 |
| `Esc` | 取消搜索 | 退出搜索模式 |

## 🎨 界面布局

```
┌──────────────────────────────────────────────────────────┐
│  OpenCode Session Manager    [快捷键提示]                │
├─────────────────────────────────┬────────────────────────┤
│ 会话列表                         │ 会话详情              │
│                                 │                        │
│ → 会话标题1                时间  │ 标题: xxx             │
│   会话标题2                时间  │ ID: xxx               │
│   会话标题3                时间  │ 目录: /path/to/project│
│                                 │ 更新: 2026-04-06      │
│                                 │ 标签: tag1 tag2       │
├─────────────────────────────────┴────────────────────────┤
```

## 📝 功能说明

### 1. 会话列表浏览

- 启动后自动显示最近更新的会话
- 显示会话标题和更新时间
- 使用 `j/k` 或 `↑/↓` 导航
- 选中的会话会有 `→` 标记和高亮显示

### 2. 会话搜索

- 按 `/` 进入搜索模式
- 输入关键词进行模糊搜索
- 按 `Enter` 确认搜索
- 按 `Esc` 取消搜索

### 3. 查看会话详情

- 右侧面板显示选中会话的详细信息
- 包含：标题、ID、目录、时间、标签、别名、备注

### 4. 继续会话

- 选中会话后按 `Enter`
- 自动调用 `opencode -s <session-id>`
- 切换到 OpenCode 继续开发

### 5. 刷新列表

- 按 `r` 重新加载会话列表
- 获取最新的会话数据

## 🔧 配置文件

配置文件位于 `~/.config/ocsession/config.toml`

### 添加标签和别名

编辑配置文件：

```toml
[session_tags.ses_2a725bdbbffeP9irDnInRMc2yQ]
tags = ["voice-input", "active-project", "production"]
notes = "语音输入功能开发，已完成基础功能"

[aliases]
voice-input = "ses_2a725bdbbffeP9irDnInRMc2yQ"
```

保存后按 `r` 刷新列表即可看到效果。

## 📊 时间显示

应用会智能显示时间格式：

- **今天**: 显示为 `15:04`（小时:分钟）
- **本周**: 显示为 `周一 15:04`
- **更早**: 显示为 `2006-01-02`

## 🎯 使用场景

### 场景1: 快速切换项目

1. 启动 `ocsession`
2. 使用 `j/k` 浏览会话
3. 找到目标会话按 `Enter` 继续

### 场景2: 查找历史会话

1. 按 `/` 进入搜索模式
2. 输入项目名或关键词
3. 选择会话按 `Enter` 继续

### 场景3: 管理活跃项目

1. 在配置文件中添加标签和别名
2. 按 `r` 刷新列表
3. 通过标签快速识别项目状态

## ⚠️ 注意事项

1. **OpenCode 必须已安装**: 应用依赖 `opencode` 命令
2. **数据库路径**: 自动使用 `~/.local/share/opencode/opencode.db`
3. **配置文件**: 首次运行会自动创建默认配置

## 🐛 问题排查

### 问题1: 按键没反应

**解决方案**: 确保在真实终端中运行，不是脚本或管道中运行

### 问题2: 会话列表为空

**解决方案**: 
- 检查 OpenCode 数据库是否存在
- 检查是否有会话记录
- 尝试按 `r` 刷新

### 问题3: 无法切换到 OpenCode

**解决方案**:
- 确认 `opencode` 命令可用
- 检查会话 ID 是否正确

## 💡 提示

- 会话按更新时间倒序排列
- 每次最多显示 50 个会话
- 标签和别名需要手动在配置文件中设置
- 时间显示会自动适配时区

---

**Happy Coding! 🎉**